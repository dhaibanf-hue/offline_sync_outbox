name: offline_sync_outbox
description: Queue API writes offline and replay them in order with persistent storage and configurable retries for Dart and Flutter.
version: 1.0.2
repository: https://github.com/dhaibanf-hue/offline_sync_outbox
issue_tracker: https://github.com/dhaibanf-hue/offline_sync_outbox/issues
topics:
  - offline-first
  - synchronization
  - queue
  - retry
  - flutter

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
